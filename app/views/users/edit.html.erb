<% provide(:title, 'Edit') %>
<h1>Editing User</h1>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
<div><%= "@user.id: #{@user.id}" %></div>
<div><%= "@user.role: #{@user.role}" %></div>